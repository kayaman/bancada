# Commits that only moved whitespace around. `git blame` skips these, so it
# keeps pointing at whoever last changed the code rather than at the reformat.
#
# Enable it once per clone (it is not automatic):
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub honours this file on its own, with no configuration.
#
# Only add a commit here if it is PURELY formatting — one semantic change
# hidden in the list becomes a change nobody can blame their way back to.

# style: cargo fmt --all — 148 sites across 19 files, rustfmt 1.9.0-stable
39a90695a5495f38fb247f41123e6a992acd0bc3
